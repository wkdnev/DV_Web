@inject NavigationManager Navigation

@code {
    protected override void OnInitialized()
    {
        // Use /auth/login which is an MVC endpoint that doesn't require auth
        Navigation.NavigateTo("/auth/login", forceLoad: true);
    }
}